dist/
build/
.venv/
.env
.DS_Store
.claude/*
!.claude/settings.json
*.spec
__pycache__/
*.pyc

# user data — never commit / clobber
portfolio.json
custom_stocks.json

portfolio.json.backup

# Broker certificates (.p12) — never commit these
local_certs/

# fubon_neo SDK log output
log/

# Live Arb tick-by-tick CSV recordings — local capture, not committed
live_tick_logs/

# Rust IV engine build output
rust/*/target/
